server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:/etc/application-configs,file:/etc/application-configs/service-config